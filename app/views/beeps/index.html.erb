<p id="notice"><%= notice %></p>

<h1 class="text-center">Beeps</h1>
<h2>Good for bots, bad for humans.</h2>

<%= turbo_stream_from "beeps" %>
<%= turbo_frame_tag "beeps" do %>
  <%= render @beeps %>
<% end %>

<%= link_to 'New Beep ->', new_beep_path, class: "w-max my-4 block flex-none bg-gray-900 hover:bg-gray-700 text-white text-lg leading-6 font-semibold py-3 px-6 border border-transparent rounded-xl focus:ring-2 focus:ring-offset-2 focus:ring-offset-white focus:ring-gray-900 focus:outline-none transition-colors duration-200" %>
